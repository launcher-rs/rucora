# agentkit-a2a（A2A 集成，可选）

`agentkit-a2a` 提供 A2A（Agent-to-Agent）方向的扩展能力，用于多 agent 协作或与外部 agent 系统集成。

## 定位

- 作为可选 crate，通过 feature 打开（见 `agentkit` 的 features）
- 不影响 core/runtime 的最小可用链路

## 使用

根据你接入的 A2A 协议/平台选择对应的集成方式。
